# SOC Beacon Environment Configuration

# Database file path (default: data/soc-beacon.db)
SOC_BEACON_DB_PATH=data/soc-beacon.db

# Required in production: used to sign and verify session cookies.
SOC_BEACON_SESSION_SECRET=

# Optional but strongly recommended: bootstrap admin password at first DB initialization.
SOC_BEACON_ADMIN_PASSWORD=

# Optional: RAG memory with Qdrant (recommended for Docker production)
# Example: http://qdrant:6333 inside docker-compose network
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=soc_beacon_alert_memory
RAG_EMBEDDING_MODEL=text-embedding-3-small

# Optional fallback key for embeddings when non-OpenAI LLM provider is selected.
OPENAI_API_KEY=
